import React, { useState, useEffect } from 'react';
import { 
  ArrowRight, User, ShoppingBag, Phone, Accessibility, X, Type, Contrast, 
  Link as LinkIcon, Menu, ChevronDown, CheckCircle2,
  MapPin, Sparkles, Globe, ChevronUp, Package, FileText, 
  LayoutDashboard, Settings, Download, Repeat, Truck, Clock, Search,
  Plus, LogOut, Mail
} from 'lucide-react';

// --- MOCK DATA ---
const mockOrders = [
  { id: 'PO-102938', date: 'Oct 15, 2026', amount: 15450, status: 'Processing', items: 34, po: 'REF-8821' },
  { id: 'PO-102801', date: 'Sep 02, 2026', amount: 8200, status: 'Shipped', items: 12, po: 'REF-8810' },
  { id: 'PO-102654', date: 'Aug 20, 2026', amount: 45000, status: 'Delivered', items: 85, po: 'LAUNCH-1' },
];

export default function App() {
  // --- STATE MANAGEMENT ---
  const [mobileMenuOpen, setMobileMenuOpen] = useState(false);
  const [activeMegaMenu, setActiveMegaMenu] = useState(null);
  const [langMenuOpen, setLangMenuOpen] = useState(false);
  const [userMenuOpen, setUserMenuOpen] = useState(false);
  const [currentLang, setCurrentLang] = useState('EN');
  
  // Dashboard State
  const [activeTab, setActiveTab] = useState('overview'); // overview, orders, invoices, settings

  // Language Flag Mapping
  const langFlags = {
    'EN': '🇬🇧',
    'CS': '🇨🇿',
    'VI': '🇻🇳'
  };

  // Scroll to Top State
  const [showScroll, setShowScroll] = useState(false);

  useEffect(() => {
    const checkScrollTop = () => {
      setShowScroll(window.scrollY > 500);
    };
    window.addEventListener('scroll', checkScrollTop);
    return () => window.removeEventListener('scroll', checkScrollTop);
  }, []);

  // Accessibility State
  const [a11yOpen, setA11yOpen] = useState(false);
  const [textSize, setTextSize] = useState('standard'); // 'small', 'standard', 'large'
  const [highContrast, setHighContrast] = useState(false);
  const [highlightLinks, setHighlightLinks] = useState(false);

  // --- DYNAMIC ACCESSIBILITY CLASSES ---
  const getBodyTextSize = () => {
    switch(textSize) {
      case 'small': return 'text-sm';
      case 'large': return 'text-lg md:text-xl';
      default: return 'text-base';
    }
  };

  const getHeadingSize = (baseClass, largeClass) => {
    return textSize === 'large' ? largeClass : baseClass;
  };

  const appClasses = `min-h-screen font-sans transition-all duration-300 ease-in-out flex flex-col ${highContrast ? 'bg-white text-black' : 'bg-[#FAFAF9] text-stone-900'}`;
  const linkClasses = highlightLinks ? 'underline decoration-2 underline-offset-4 font-bold' : '';
  const btnClasses = highContrast 
    ? 'bg-black text-white hover:bg-stone-800 border-2 border-black disabled:opacity-50' 
    : 'bg-stone-900 text-white hover:bg-amber-700 shadow-md transition-colors duration-300 disabled:opacity-50 disabled:bg-stone-300 disabled:shadow-none';
  const cardClasses = highContrast 
    ? 'bg-white border-4 border-black' 
    : 'bg-white border border-stone-200 shadow-sm transition-shadow duration-300';
  const inputClasses = `w-full px-5 py-4 rounded-xl border transition-all focus:outline-none focus:ring-2 ${
    highContrast 
      ? 'bg-white border-black border-[3px] focus:ring-black text-black' 
      : 'bg-stone-50 border-stone-200 focus:border-amber-600 focus:ring-amber-600/20 focus:bg-white text-stone-900'
  }`;

  // Helper for Order Status
  const renderStatus = (status) => {
    switch(status) {
      case 'Processing':
        return <span className={`inline-flex items-center gap-1.5 px-3 py-1 rounded-md text-xs font-bold uppercase tracking-wider ${highContrast ? 'bg-black text-white' : 'bg-amber-100 text-amber-800'}`}><Clock className="w-3.5 h-3.5"/> Processing</span>;
      case 'Shipped':
        return <span className={`inline-flex items-center gap-1.5 px-3 py-1 rounded-md text-xs font-bold uppercase tracking-wider ${highContrast ? 'bg-black text-white' : 'bg-blue-100 text-blue-800'}`}><Truck className="w-3.5 h-3.5"/> Shipped</span>;
      case 'Delivered':
        return <span className={`inline-flex items-center gap-1.5 px-3 py-1 rounded-md text-xs font-bold uppercase tracking-wider ${highContrast ? 'bg-black text-white' : 'bg-green-100 text-green-800'}`}><CheckCircle2 className="w-3.5 h-3.5"/> Delivered</span>;
      default:
        return <span>{status}</span>;
    }
  };

  return (
    <div className={appClasses}>
      
      {/* --- ACCESSIBILITY PANEL --- */}
      <div className={`fixed inset-y-0 right-0 w-80 md:w-96 bg-stone-900 text-white z-[100] transform transition-transform duration-300 shadow-2xl ${a11yOpen ? 'translate-x-0' : 'translate-x-full'}`}>
        <div className="p-6 flex items-center justify-between border-b border-stone-800">
          <h2 className="text-xl font-bold flex items-center gap-2"><Accessibility className="w-6 h-6 text-amber-500" /> Accessibility</h2>
          <button onClick={() => setA11yOpen(false)} className="p-2 hover:bg-stone-800 rounded-md transition-colors"><X className="w-6 h-6" /></button>
        </div>
        <div className="p-6 space-y-8">
          <div>
            <h3 className="font-bold text-sm uppercase tracking-widest text-stone-300 mb-4 flex items-center gap-3"><Type className="w-5 h-5"/> Text Size</h3>
            <div className="flex flex-col gap-2 bg-stone-800 rounded-xl p-2 font-bold">
              <button onClick={() => setTextSize('small')} className={`w-full py-3 rounded-lg transition-colors ${textSize === 'small' ? 'bg-amber-600 text-white' : 'text-stone-400 hover:text-white'}`}>Small</button>
              <button onClick={() => setTextSize('standard')} className={`w-full py-3 rounded-lg transition-colors ${textSize === 'standard' ? 'bg-amber-600 text-white' : 'text-stone-400 hover:text-white'}`}>Standard</button>
              <button onClick={() => setTextSize('large')} className={`w-full py-3 rounded-lg transition-colors ${textSize === 'large' ? 'bg-amber-600 text-white' : 'text-stone-400 hover:text-white'}`}>Large</button>
            </div>
          </div>
          <div>
            <h3 className="font-bold text-sm uppercase tracking-widest text-stone-300 mb-4 flex items-center gap-3"><Contrast className="w-5 h-5"/> Contrast</h3>
            <label className="flex items-center justify-between cursor-pointer bg-stone-800 p-5 rounded-xl font-bold hover:bg-stone-700 transition-colors">
              <span>High Contrast (B/W)</span>
              <div className={`w-14 h-8 rounded-full relative transition-colors ${highContrast ? 'bg-amber-500' : 'bg-stone-600'}`}>
                <div className={`absolute top-1 left-1 w-6 h-6 rounded-full bg-white transition-transform ${highContrast ? 'translate-x-6' : ''}`}></div>
              </div>
              <input type="checkbox" className="hidden" checked={highContrast} onChange={() => setHighContrast(!highContrast)}/>
            </label>
          </div>
          <div>
            <h3 className="font-bold text-sm uppercase tracking-widest text-stone-300 mb-4 flex items-center gap-3"><LinkIcon className="w-5 h-5"/> Navigation</h3>
            <label className="flex items-center justify-between cursor-pointer bg-stone-800 p-5 rounded-xl font-bold hover:bg-stone-700 transition-colors">
              <span>Highlight Links</span>
              <div className={`w-14 h-8 rounded-full relative transition-colors ${highlightLinks ? 'bg-amber-500' : 'bg-stone-600'}`}>
                <div className={`absolute top-1 left-1 w-6 h-6 rounded-full bg-white transition-transform ${highlightLinks ? 'translate-x-6' : ''}`}></div>
              </div>
              <input type="checkbox" className="hidden" checked={highlightLinks} onChange={() => setHighlightLinks(!highlightLinks)}/>
            </label>
          </div>
        </div>
      </div>
      {a11yOpen && <div className="fixed inset-0 bg-black/60 z-[90] backdrop-blur-sm transition-opacity" onClick={() => setA11yOpen(false)}></div>}

      {/* --- TOP B2B NOTIFICATION (LOGGED IN) --- */}
      <div className={`py-2.5 px-6 flex justify-between items-center text-xs font-bold uppercase tracking-widest ${highContrast ? 'bg-black text-white border-b-2 border-white' : 'bg-amber-900 text-amber-50'}`}>
        <span>Partner: Luxury Retail s.r.o.</span>
        <span className="hidden sm:inline">Discount Tier: Gold (15%)</span>
      </div>

      {/* --- HEADER --- */}
      <header className={`sticky top-0 z-50 transition-colors ${highContrast ? 'bg-white border-black border-b-4' : 'bg-white border-b border-stone-200 shadow-sm'}`}>
        <div className="max-w-7xl mx-auto px-4 sm:px-6 h-20 sm:h-24 flex items-center justify-between">
          <div className="flex items-center gap-4 lg:gap-12">
            <button className="lg:hidden p-2" onClick={() => setMobileMenuOpen(true)}><Menu className="w-8 h-8" /></button>
            <h1 className="font-serif text-2xl sm:text-3xl font-bold cursor-pointer">Leather Parfum</h1>
            
            <nav className="hidden lg:flex items-center gap-8 font-bold h-24 text-sm uppercase tracking-wider">
              <div className="relative h-full flex items-center cursor-pointer group" onMouseEnter={() => setActiveMegaMenu('catalog')} onMouseLeave={() => setActiveMegaMenu(null)}>
                <button className={`flex items-center gap-1 hover:text-amber-700 transition-colors ${linkClasses}`}>
                  Order Portal <ChevronDown className="w-5 h-5"/>
                </button>
                {activeMegaMenu === 'catalog' && (
                  <div className={`absolute top-24 left-0 w-[700px] rounded-b-2xl p-10 grid grid-cols-2 gap-12 shadow-2xl cursor-default ${highContrast ? 'bg-white border-4 border-t-0 border-black' : 'bg-white border border-t-0 border-stone-200'}`}>
                    <div>
                      <h3 className="font-bold text-sm uppercase tracking-widest mb-6 pb-3 border-b border-stone-200 text-stone-900">Perfumes</h3>
                      <ul className="space-y-4 text-base normal-case tracking-normal font-medium text-stone-600">
                        <li><a href="#" className={`hover:text-amber-700 ${linkClasses}`}>All Fragrances</a></li>
                        <li><a href="#" className={`hover:text-amber-700 ${linkClasses}`}>Women's Collection</a></li>
                        <li><a href="#" className={`hover:text-amber-700 ${linkClasses}`}>Men's Collection</a></li>
                      </ul>
                    </div>
                    <div>
                      <h3 className="font-bold text-sm uppercase tracking-widest mb-6 pb-3 border-b border-stone-200 text-stone-900">Leather Goods</h3>
                      <ul className="space-y-4 text-base normal-case tracking-normal font-medium text-stone-600">
                        <li><a href="#" className={`hover:text-amber-700 ${linkClasses}`}>All Leather Goods</a></li>
                        <li><a href="#" className={`hover:text-amber-700 ${linkClasses}`}>Premium Belts</a></li>
                      </ul>
                    </div>
                  </div>
                )}
              </div>
              <a href="#" className={`hover:text-amber-700 transition-colors ${linkClasses}`}>Quick Reorder</a>
              <a href="#" className={`text-amber-700 transition-colors border-b-2 ${highContrast ? 'border-black' : 'border-amber-700'} pb-1 ${linkClasses}`}>My Account</a>
            </nav>
          </div>

          <div className="flex items-center gap-4 sm:gap-6">
            <div className="relative hidden md:block" onMouseEnter={() => setLangMenuOpen(true)} onMouseLeave={() => setLangMenuOpen(false)}>
              <button className={`flex items-center gap-1.5 font-bold text-sm uppercase tracking-wider p-2 hover:text-amber-700 transition-colors ${linkClasses}`}>
                <span className="text-lg leading-none">{langFlags[currentLang]}</span> {currentLang} <ChevronDown className="w-4 h-4"/>
              </button>
              {langMenuOpen && (
                <div className={`absolute top-full right-0 mt-2 w-32 rounded-xl shadow-lg overflow-hidden ${highContrast ? 'bg-white border-4 border-black' : 'bg-white border border-stone-200'}`}>
                  <button onClick={() => setCurrentLang('EN')} className="w-full text-left px-4 py-3 text-sm font-bold hover:bg-stone-100 transition-colors flex items-center justify-between">English <span>🇬🇧</span></button>
                  <button onClick={() => setCurrentLang('CS')} className="w-full text-left px-4 py-3 text-sm font-bold hover:bg-stone-100 transition-colors flex items-center justify-between">Čeština <span>🇨🇿</span></button>
                  <button onClick={() => setCurrentLang('VI')} className="w-full text-left px-4 py-3 text-sm font-bold hover:bg-stone-100 transition-colors flex items-center justify-between">Tiếng Việt <span>🇻🇳</span></button>
                </div>
              )}
            </div>
            <button onClick={() => setA11yOpen(true)} className={`p-2 sm:px-4 sm:py-2 rounded-xl flex items-center gap-2 font-bold ${highContrast ? 'border-2 border-black hover:bg-gray-100' : 'bg-stone-100 hover:bg-stone-200 transition-colors'}`}>
              <Accessibility className="w-6 h-6 sm:w-5 sm:h-5" />
            </button>
            <div className={`w-px h-8 hidden md:block ${highContrast ? 'bg-black' : 'bg-stone-300'}`}></div>
            
            {/* User Account Dropdown */}
            <div className="relative" onMouseEnter={() => setUserMenuOpen(true)} onMouseLeave={() => setUserMenuOpen(false)}>
              <button className={`hidden sm:flex items-center gap-2 font-bold hover:text-amber-700 transition-colors text-sm uppercase tracking-wider ${linkClasses}`}>
                <User className="w-5 h-5" /> <span className="hidden xl:inline">My Account</span> <ChevronDown className="w-4 h-4"/>
              </button>
              {userMenuOpen && (
                <div className={`absolute top-full right-0 mt-2 w-48 rounded-xl shadow-lg overflow-hidden ${highContrast ? 'bg-white border-4 border-black' : 'bg-white border border-stone-200'}`}>
                  <div className="px-4 py-3 border-b border-stone-100">
                    <p className="text-xs font-bold text-stone-400 uppercase tracking-widest mb-1">Logged in as</p>
                    <p className="text-sm font-bold truncate">John Doe</p>
                  </div>
                  <button className="w-full text-left px-4 py-3 text-sm font-bold hover:bg-stone-100 transition-colors flex items-center gap-2"><LayoutDashboard className="w-4 h-4"/> Dashboard</button>
                  <button className="w-full text-left px-4 py-3 text-sm font-bold hover:bg-stone-100 transition-colors flex items-center gap-2 text-red-600"><LogOut className="w-4 h-4"/> Sign Out</button>
                </div>
              )}
            </div>

            <button className={`relative p-2 font-bold hover:text-amber-700 transition-colors ${linkClasses}`}>
              <ShoppingBag className="w-7 h-7 sm:w-6 sm:h-6" />
            </button>
          </div>
        </div>
      </header>

      {/* --- MOBILE FULLSCREEN MENU --- */}
      <div className={`fixed inset-0 z-[110] flex flex-col transition-transform duration-300 ${mobileMenuOpen ? 'translate-x-0' : '-translate-x-full'} ${highContrast ? 'bg-white text-black' : 'bg-stone-50 text-stone-900'}`}>
        <div className={`h-20 sm:h-24 px-4 sm:px-6 flex items-center justify-between border-b ${highContrast ? 'border-black border-b-4' : 'border-stone-200 bg-white'}`}>
          <span className="font-serif text-3xl font-bold">Menu</span>
          <button onClick={() => setMobileMenuOpen(false)} className="p-2"><X className="w-8 h-8" /></button>
        </div>
        <div className="flex-grow flex flex-col py-8 px-6 gap-6 overflow-y-auto font-medium">
          <div className="pb-4 border-b border-stone-200">
            <p className="text-xs font-bold text-stone-400 uppercase tracking-widest mb-1">Partner</p>
            <p className="text-lg font-bold">Luxury Retail s.r.o.</p>
          </div>
          <a href="#" className={`text-left text-2xl text-amber-700 py-2 ${linkClasses}`}>Order Portal</a>
          <a href="#" className={`text-left text-2xl py-2 ${linkClasses}`}>Quick Reorder</a>
          <a href="#" className={`text-left text-2xl py-2 ${linkClasses}`}>Invoices</a>
          <div className={`h-px w-full my-4 ${highContrast ? 'bg-black' : 'bg-stone-200'}`}></div>
          <button className="flex items-center gap-3 text-xl py-2 mt-4 font-bold text-red-600"><LogOut className="w-6 h-6" /> Sign Out</button>
          
          <div className="mt-auto pt-8 flex items-center gap-3 opacity-60">
            <Globe className="w-5 h-5" />
            <select className="bg-transparent font-bold outline-none text-lg" value={currentLang} onChange={(e) => setCurrentLang(e.target.value)}>
              <option value="EN">English</option>
              <option value="CS">Čeština</option>
              <option value="VI">Tiếng Việt</option>
            </select>
          </div>
        </div>
      </div>

      {/* --- DASHBOARD MAIN CONTENT --- */}
      <main className="flex-grow w-full bg-stone-50">
        
        {/* Dashboard Header */}
        <section className={`py-10 px-4 sm:px-6 ${highContrast ? 'border-b-4 border-black bg-white' : 'bg-white border-b border-stone-200'}`}>
          <div className="max-w-7xl mx-auto flex flex-col md:flex-row md:items-end justify-between gap-6">
            <div>
              <h1 className={`font-serif ${getHeadingSize('text-4xl', 'text-5xl')} font-bold mb-2`}>
                Partner Dashboard
              </h1>
              <p className={`${getBodyTextSize()} ${highContrast ? 'font-medium text-black' : 'text-stone-500'}`}>
                Welcome back, Anna. Here's what's happening with your account today.
              </p>
            </div>
            <button className={`px-6 py-4 rounded-xl font-bold flex items-center justify-center gap-2 ${btnClasses}`}>
              <Plus className="w-5 h-5" /> New Order
            </button>
          </div>
        </section>

        <section className="px-4 sm:px-6 py-12 max-w-7xl mx-auto">
          <div className="flex flex-col lg:flex-row gap-10 items-start">
            
            {/* LEFT COLUMN: Sidebar Navigation */}
            <aside className={`w-full lg:w-64 shrink-0 lg:sticky lg:top-32`}>
              <nav className="flex flex-row lg:flex-col gap-2 overflow-x-auto lg:overflow-visible pb-4 lg:pb-0">
                {[
                  { id: 'overview', icon: <LayoutDashboard className="w-5 h-5"/>, label: 'Overview' },
                  { id: 'orders', icon: <Package className="w-5 h-5"/>, label: 'Order History' },
                  { id: 'invoices', icon: <FileText className="w-5 h-5"/>, label: 'Invoices' },
                  { id: 'settings', icon: <Settings className="w-5 h-5"/>, label: 'Account Settings' },
                ].map(item => (
                  <button
                    key={item.id}
                    onClick={() => setActiveTab(item.id)}
                    className={`flex items-center gap-3 px-5 py-4 rounded-xl font-bold transition-colors whitespace-nowrap ${
                      activeTab === item.id 
                        ? (highContrast ? 'bg-black text-white border-2 border-black' : 'bg-amber-100 text-amber-900') 
                        : (highContrast ? 'bg-white text-black border-2 border-transparent hover:border-black' : 'text-stone-600 hover:bg-stone-200')
                    }`}
                  >
                    {item.icon} {item.label}
                  </button>
                ))}
              </nav>
            </aside>

            {/* RIGHT COLUMN: Tab Content */}
            <div className="flex-grow w-full">
              
              {activeTab === 'overview' && (
                <div className="space-y-8 lg:space-y-12">
                  
                  {/* Overview Cards */}
                  <div className="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-6">
                    <div className={`${cardClasses} p-6 sm:p-8 rounded-3xl flex flex-col`}>
                      <div className="flex items-center justify-between mb-6">
                        <div className={`w-12 h-12 rounded-xl flex items-center justify-center ${highContrast ? 'border-2 border-black' : 'bg-amber-50 text-amber-700'}`}>
                          <Sparkles className="w-6 h-6" />
                        </div>
                        <span className={`text-xs font-bold uppercase tracking-widest px-3 py-1 rounded-md ${highContrast ? 'bg-black text-white' : 'bg-stone-100 text-stone-600'}`}>Active</span>
                      </div>
                      <p className={`text-sm font-bold uppercase tracking-widest mb-1 ${highContrast ? 'text-black' : 'text-stone-500'}`}>Discount Tier</p>
                      <h3 className={`font-serif ${getHeadingSize('text-3xl', 'text-4xl')} font-bold`}>Gold (15%)</h3>
                      <div className={`mt-6 pt-4 border-t text-sm font-medium ${highContrast ? 'border-black' : 'border-stone-200 text-stone-500'}`}>
                        Unlock Platinum (20%) by spending 100k CZK.
                      </div>
                    </div>

                    <div className={`${cardClasses} p-6 sm:p-8 rounded-3xl flex flex-col`}>
                      <div className="flex items-center justify-between mb-6">
                        <div className={`w-12 h-12 rounded-xl flex items-center justify-center ${highContrast ? 'border-2 border-black' : 'bg-blue-50 text-blue-700'}`}>
                          <User className="w-6 h-6" />
                        </div>
                      </div>
                      <p className={`text-sm font-bold uppercase tracking-widest mb-1 ${highContrast ? 'text-black' : 'text-stone-500'}`}>Account Manager</p>
                      <h3 className={`font-serif ${getHeadingSize('text-3xl', 'text-4xl')} font-bold`}>Anna Dvořáková</h3>
                      <div className={`mt-6 pt-4 border-t text-sm font-bold flex items-center gap-4 ${highContrast ? 'border-black' : 'border-stone-200 text-stone-600'}`}>
                        <button className="flex items-center gap-2 hover:text-amber-700"><Mail className="w-4 h-4"/> Email</button>
                        <button className="flex items-center gap-2 hover:text-amber-700"><Phone className="w-4 h-4"/> Call</button>
                      </div>
                    </div>

                    <div className={`${cardClasses} p-6 sm:p-8 rounded-3xl flex flex-col md:col-span-2 xl:col-span-1`}>
                      <div className="flex items-center justify-between mb-6">
                        <div className={`w-12 h-12 rounded-xl flex items-center justify-center ${highContrast ? 'border-2 border-black' : 'bg-green-50 text-green-700'}`}>
                          <Package className="w-6 h-6" />
                        </div>
                      </div>
                      <p className={`text-sm font-bold uppercase tracking-widest mb-1 ${highContrast ? 'text-black' : 'text-stone-500'}`}>YTD Purchases</p>
                      <h3 className={`font-serif ${getHeadingSize('text-3xl', 'text-4xl')} font-bold`}>68,650 CZK</h3>
                      <div className={`mt-6 pt-4 border-t text-sm font-medium ${highContrast ? 'border-black' : 'border-stone-200 text-stone-500'}`}>
                        Total for the year 2026.
                      </div>
                    </div>
                  </div>

                  {/* Recent Orders Section */}
                  <div>
                    <div className="flex justify-between items-end mb-6">
                      <h3 className={`font-serif ${getHeadingSize('text-2xl', 'text-3xl')} font-bold`}>Recent Orders</h3>
                      <button onClick={() => setActiveTab('orders')} className={`text-sm font-bold uppercase tracking-widest hover:text-amber-700 transition-colors ${linkClasses}`}>
                        View All
                      </button>
                    </div>
                    
                    <div className={`rounded-3xl overflow-hidden ${cardClasses}`}>
                      {/* Desktop Table Header */}
                      <div className={`hidden md:grid grid-cols-12 gap-4 p-6 border-b text-xs font-bold uppercase tracking-widest ${highContrast ? 'border-black bg-white' : 'border-stone-200 bg-stone-50 text-stone-500'}`}>
                        <div className="col-span-3">Order ID / PO</div>
                        <div className="col-span-2">Date</div>
                        <div className="col-span-2">Status</div>
                        <div className="col-span-2 text-right">Amount</div>
                        <div className="col-span-3 text-right">Actions</div>
                      </div>

                      {/* Orders List */}
                      <div className="divide-y divide-stone-200">
                        {mockOrders.map((order) => (
                          <div key={order.id} className={`p-6 flex flex-col md:grid md:grid-cols-12 md:gap-4 md:items-center ${highContrast ? 'divide-y divide-black md:divide-y-0' : ''}`}>
                            
                            {/* Mobile/Desktop Data */}
                            <div className="md:col-span-3 mb-4 md:mb-0 flex justify-between md:block">
                              <span className="md:hidden font-bold uppercase text-xs text-stone-500">Order ID:</span>
                              <div>
                                <div className="font-bold text-lg">{order.id}</div>
                                <div className="text-xs font-medium uppercase tracking-widest text-stone-500">{order.po}</div>
                              </div>
                            </div>
                            
                            <div className="md:col-span-2 py-3 md:py-0 flex justify-between md:block">
                              <span className="md:hidden font-bold uppercase text-xs text-stone-500">Date:</span>
                              <div className={`font-medium ${getBodyTextSize()}`}>{order.date}</div>
                            </div>
                            
                            <div className="md:col-span-2 py-3 md:py-0 flex justify-between md:block">
                              <span className="md:hidden font-bold uppercase text-xs text-stone-500">Status:</span>
                              <div>{renderStatus(order.status)}</div>
                            </div>
                            
                            <div className="md:col-span-2 py-3 md:py-0 flex justify-between md:block md:text-right">
                              <span className="md:hidden font-bold uppercase text-xs text-stone-500">Amount:</span>
                              <div className="font-bold">{order.amount.toLocaleString()} CZK</div>
                            </div>
                            
                            <div className="md:col-span-3 pt-4 md:pt-0 mt-2 md:mt-0 flex flex-col sm:flex-row md:justify-end gap-3">
                              <button className={`px-4 py-2 text-sm font-bold rounded-lg flex items-center justify-center gap-2 transition-colors ${highContrast ? 'border-2 border-black hover:bg-black hover:text-white' : 'bg-stone-100 hover:bg-stone-200 text-stone-900'}`}>
                                <Download className="w-4 h-4" /> <span className="md:hidden xl:inline">Invoice</span>
                              </button>
                              <button className={`px-4 py-2 text-sm font-bold rounded-lg flex items-center justify-center gap-2 transition-colors ${highContrast ? 'bg-black text-white hover:bg-stone-800' : 'bg-stone-900 text-white hover:bg-stone-800'}`}>
                                <Repeat className="w-4 h-4" /> <span className="md:hidden xl:inline">Reorder</span>
                              </button>
                            </div>

                          </div>
                        ))}
                      </div>
                    </div>
                  </div>

                </div>
              )}

              {/* Placeholder for other tabs */}
              {activeTab !== 'overview' && (
                <div className={`p-16 text-center rounded-3xl ${cardClasses}`}>
                  <Search className="w-16 h-16 mx-auto mb-6 text-stone-300" />
                  <h3 className={`font-serif ${getHeadingSize('text-2xl', 'text-3xl')} font-bold mb-4 capitalize`}>{activeTab} Panel</h3>
                  <p className={`${getBodyTextSize()} text-stone-500 mb-8`}>This section is currently under development.</p>
                  <button onClick={() => setActiveTab('overview')} className={`px-8 py-4 rounded-xl font-bold ${btnClasses}`}>Back to Overview</button>
                </div>
              )}

            </div>
          </div>
        </section>

      </main>

      {/* --- SCROLL TO TOP BUTTON --- */}
      {showScroll && (
        <button 
          onClick={() => window.scrollTo({ top: 0, behavior: 'smooth' })}
          className={`fixed bottom-8 right-8 z-[80] p-4 rounded-full shadow-2xl transition-all hover:-translate-y-1 focus:outline-none ${highContrast ? 'bg-black text-white border-2 border-white' : 'bg-stone-900 text-white hover:bg-amber-700'}`}
          aria-label="Scroll to top"
        >
          <ChevronUp className="w-6 h-6" />
        </button>
      )}

      {/* --- FOOTER --- */}
      <footer className={`${highContrast ? 'bg-white border-t-4 border-black text-black' : 'bg-stone-900 text-stone-300'} py-16 md:py-24 mt-auto`}>
        <div className="max-w-7xl mx-auto px-6 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-12 lg:gap-8 mb-16">
          <div className="lg:col-span-1">
            <h4 className="font-serif text-3xl font-bold mb-6 text-white">Leather Parfum</h4>
            <p className={`${getBodyTextSize()} font-medium opacity-80 mb-8`}>
              Your premium wholesale partner. Exclusive terms for retail businesses.
            </p>
          </div>
          <div>
            <h4 className="text-sm font-bold mb-8 uppercase tracking-widest text-stone-500">Navigation</h4>
            <ul className={`space-y-5 font-bold ${getBodyTextSize()}`}>
              <li><a href="#" className={`text-amber-500 transition-colors ${linkClasses}`}>Order Portal</a></li>
              <li><a href="#" className={`hover:text-amber-500 transition-colors ${linkClasses}`}>Quick Reorder</a></li>
              <li><a href="#" className={`hover:text-amber-500 transition-colors ${linkClasses}`}>Invoices & Documents</a></li>
            </ul>
          </div>
          <div>
            <h4 className="text-sm font-bold mb-8 uppercase tracking-widest text-stone-500">Contact Manager</h4>
            <ul className={`space-y-5 font-bold ${getBodyTextSize()}`}>
              <li className="flex items-center gap-3"><User className="w-5 h-5 opacity-50"/> Anna Dvořáková</li>
              <li className="flex items-center gap-3"><Phone className="w-5 h-5 opacity-50"/> +420 123 456 789</li>
              <li className="flex items-center gap-3">anna@leatherparfum.com</li>
            </ul>
          </div>
          <div>
            <h4 className="text-sm font-bold mb-8 uppercase tracking-widest text-stone-500">Legal</h4>
            <ul className={`space-y-5 font-bold ${getBodyTextSize()}`}>
              <li><a href="#" className={`hover:text-amber-500 transition-colors ${linkClasses}`}>B2B Terms of Service</a></li>
              <li><a href="#" className={`hover:text-amber-500 transition-colors ${linkClasses}`}>Returns & Claims</a></li>
            </ul>
          </div>
        </div>
        <div className={`max-w-7xl mx-auto px-6 pt-8 flex flex-col md:flex-row justify-between items-center gap-4 text-sm font-bold uppercase tracking-wider ${highContrast ? 'border-t-2 border-black' : 'border-t border-stone-800'}`}>
          <p className="opacity-60">&copy; 2026 Leather Parfum Wholesale. All rights reserved.</p>
          <div className="flex gap-6 opacity-60">
            <span>Made in Europe</span>
          </div>
        </div>
      </footer>

    </div>
  );
}